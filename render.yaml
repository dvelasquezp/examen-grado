# Blueprint Render — revisión en la nube (Docker)

services:
  - type: web
    name: examen-api
    runtime: docker
    plan: free
    dockerfilePath: ./apps/api/Dockerfile.cloud
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DATABASE_URL_SYNC
        sync: false
      - key: DEBUG
        value: "false"
      - key: NEO4J_URI
        value: bolt://localhost:7687
      - key: REDIS_URL
        value: redis://localhost:6379/0
      - key: EMBEDDING_ENABLED
        value: "false"
      - key: CORS_ORIGINS
        value: examen-web.onrender.com
      - key: CONTENT_PATH
        value: .
      - key: APP_ENV
        value: production

  - type: web
    name: examen-web
    runtime: docker
    plan: free
    dockerfilePath: ./apps/web/Dockerfile
    dockerContext: ./apps/web
    healthCheckPath: /
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://examen-api.onrender.com/api/v1
